<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kero-Stor | إنشاء حساب</title>
    <style>
        /* ده المكان اللي بيتحط فيه كود التنسيق عشان يشتغل صح */
        body {
            background: #f0f2f5;
            font-family: 'Segoe UI', Tahoma, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .signup-card {
            background: white;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
            width: 350px;
            text-align: center;
        }
        .brand-name { font-weight: bold; color: #e67e22; font-size: 24px; }
        input, select {
            width: 100%;
            padding: 12px;
            margin: 10px 0;
            border: 1px solid #ddd;
            border-radius: 10px;
        }
        .btn-submit {
            width: 100%;
            padding: 12px;
            background: #e67e22;
            color: white;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="signup-card">
        <div class="brand-name">KERO-STOR</div>
        <h2>إنشاء حساب جديد</h2>
        <p>تجارة أيمن لمعي - بإشراف كيرلس أيمن</p>
        <form>
            <input type="text" placeholder="الاسم الكامل" required>
            <input type="tel" placeholder="رقم الهاتف" required>
            <select required>
                <option value="" disabled selected>اختر نوع الحساب</option>
                 <option>عامل</option>
                <option>شريك</option>
                <option>تاجر الحافظة</option>
            </select>
            <button type="submit" class="btn-submit">تسجيل البيانات</button>
        </form>
    </div>
</body>
</html>

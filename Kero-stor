<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kero-Stor | إنشاء حساب</title>
    <style>
        body {
            background-color: #121212; /* خلفية سوداء فخمة */
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            color: white;
        }
        .container {
            background: #1e1e1e;
            padding: 30px;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
            width: 90%;
            max-width: 400px;
            text-align: center;
            border: 1px solid #333;
        }
        /* تظبيط صورتك اللي في الملف */
        .profile-img {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            object-fit: cover;
            border: 3px solid #e67e22;
            margin-bottom: 15px;
        }
        h2 { color: #e67e22; margin-bottom: 5px; }
        .subtitle { color: #aaa; margin-bottom: 25px; font-size: 14px; }
        input, select {
            width: 100%;
            padding: 12px;
            margin-bottom: 15px;
            border-radius: 10px;
            border: 1px solid #444;
            background: #2b2b2b;
            color: white;
            box-sizing: border-box;
        }
        .btn {
            width: 100%;
            padding: 12px;
            background: #e67e22;
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 18px;
            font-weight: bold;
            cursor: pointer;
            transition: 0.3s;
        }
        .btn:hover { background: #d35400; }
    </style>
</head>
<body>

    <div class="container">
        <img src="kero.jpg" alt="كيرلس أيمن" class="profile-img">
        
        <h2>KERO-STOR</h2>
        <div class="subtitle">تجارة أيمن لمعي - بإشراف كيرلس أيمن</div>

        <form>
            <input type="text" placeholder="الاسم الكامل" required>
            <input type="tel" placeholder="رقم الهاتف" required>
            <select required>
                <option value="" disabled selected>اختر نوع الحساب</option>
                <option>عامل</option>
                <option>شريك</option>
                <option>تاجر الحافظة</option>
            </select>
            <button type="submit" class="btn">إنشاء حساب</button>
        </form>
    </div>

</body>
</html>

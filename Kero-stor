<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kero-Stor | كيرلس أيمن</title>
    <style>
        body {
            background: #1a1a1a; /* خلفية سوداء شيك */
            font-family: 'Segoe UI', Tahoma, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            color: white;
        }
        .container {
            background: #262626;
            padding: 30px;
            border-radius: 25px;
            box-shadow: 0 20px 50px rgba(0,0,0,0.5);
            width: 380px;
            text-align: center;
            border: 1px solid #444;
        }
        /* تظبيط صورتك الشخصية */
        .profile-img {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            object-fit: cover;
            border: 4px solid #e67e22;
            margin-bottom: 15px;
            box-shadow: 0 0 15px #e67e22;
        }
        h2 { color: #e67e22; margin: 10px 0; }
        p { color: #ccc; font-size: 14px; margin-bottom: 20px; }
        
        input, select {
            width: 100%;
            padding: 12px;
            margin-bottom: 15px;
            border-radius: 10px;
            border: none;
            background: #333;
            color: white;
            font-size: 16px;
        }
        .btn {
            width: 100%;
            padding: 12px;
            background: #e67e22;
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 18px;
            font-weight: bold;
            cursor: pointer;
            transition: 0.3s;
        }
        .btn:hover { background: #d35400; }
    </style>
</head>
<body>

    <div class="container">
        <img src="kero-photo.jpg" alt="كيرلس أيمن" class="profile-img">
        
        <h2>KERO-STOR</h2>
        <p>تجارة أيمن لمعي - بإشراف كيرلس أيمن</p>

        <form>
            <input type="text" placeholder="الاسم الكامل" required>
            <input type="tel" placeholder="رقم الهاتف" required>
            <select required>
                <option value="" disabled selected>اختر نوع الحساب</option>
                <option>عامل</option>
                <option>شريك</option>
                <option>تاجر الحافظة</option>
            </select>
            <button type="submit" class="btn">إنشاء حساب</button>
        </form>
    </div>

</body>
</html>
